{
  "name": "modalx-docs",
  "version": "0.1.1",
  "private": true,
  "description": "Official documentation for modalx - The responsive terminal modal framework for Rust",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "deploy": "npm run docs:build"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
